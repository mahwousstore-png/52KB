[build]
# استخدام Dockerfile بدل Railpack/Nixpacks لتجنب مشكلة secrets أثناء البناء
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
healthcheckPath = "/"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
